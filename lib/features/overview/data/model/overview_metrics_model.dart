/// Live metric counts for the Overview dashboard cards.
class OverviewMetricsModel {
  final int totalOpen;
  final int createdThisWeek;

  const OverviewMetricsModel({
    required this.totalOpen,
    required this.createdThisWeek,
  });
}
